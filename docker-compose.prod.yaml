services:
  frontend:
    volumes:
      - ./frontend/nginx/prod.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "80:80"
      - "443:443"

  backend:
    ports:
      - "8080:8080"

  db:
    ports:
      - "${POSTGRES_PORT}:5432"
